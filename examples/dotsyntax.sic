print("Dot syntax example:|-");
strings.newline();

table = [];

table.sub = [];

table.sub.text = "Sup";

table.sub.func -> {
    print(args.0.sub.text);
}

table.sub.shock -> {
    args.0.sub.func(table);
}

table.sub.shock(table);
